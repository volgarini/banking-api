spring:
  application:
    name: banking-api
  datasource:
    url: jdbc:postgresql://localhost:5432/banking
    username: banking
    password: banking
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate